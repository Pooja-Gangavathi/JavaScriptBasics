var str = ['Hare','Krishna','hare','Ram'];
str.shift();//shift()is to remove the first element of the array
console.log(str);
str.unshift('Namaste');//unshift() is to add the new element to the array
console.log(str);

//shift() and unshift() works at the begining of the array